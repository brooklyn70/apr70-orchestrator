# TASKS — apr70-orchestrator (v1 demo backlog)

Once the orchestrator's main loop matures, it'll consume tasks from the **v3
site repo's** `TASKS.md` (mounted at `/work/TASKS.md` inside the container).
This file is just for v1 smoke-testing.

## v1 smoke test

- [ ] Print "orchestrator v1 hello" to stdout — proves Claude Code subprocess works end-to-end.

## Done

(Empty — first run hasn't happened yet.)
